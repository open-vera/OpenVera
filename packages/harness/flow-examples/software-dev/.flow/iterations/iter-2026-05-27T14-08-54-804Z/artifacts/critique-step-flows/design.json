{
  "critique": {
    "confidence": 0,
    "issues": [
      "[critical] 设计步骤的输入缺失。根据流程定义，设计步骤的输入是 `flows/requirement/output/`，但该目录为空，且未找到 `../project/docs/` 中的需求分析产物（如 `prd.md`, `user-stories.md`）。设计步骤缺少必要的输入，无法开始工作。 -> 必须先确保需求分析步骤（flows/requirement）已正确完成并将其产物（`../project/docs/prd.md`, `../project/docs/user-stories.md`）写入指定目录。请检查并重新运行需求分析步骤，或确认其输出路径是否正确。",
      "[critical] 设计步骤未生成任何交付产物。根据准出标准，本步骤应在 `../project/docs/` 目录下生成 `architecture.md`, `api-design.md`, `ui-design.md` 三个核心设计文档，并在 `./output/` 目录下生成 `summary.md` 执行摘要。当前这些文件均不存在。 -> 在获得有效的输入后，必须执行设计工作，生成所有要求的交付文档。设计内容需覆盖所有P0功能模块，并满足其他准出标准。"
    ],
    "missingChecks": [
      "确保输入可用：确认需求分析步骤（flows/requirement）已成功执行，并将其产物 `prd.md` 和 `user-stories.md` 正确写入 `../project/docs/` 目录。",
      "执行设计工作：在输入就绪后，基于需求分析产物，执行架构设计、API设计和UI设计，并将 `architecture.md`, `api-design.md`, `ui-design.md` 写入 `../project/docs/`，将 `summary.md` 写入 `./flows/design/output/`。"
    ],
    "nextAction": "replan",
    "rationale": "设计步骤（flows/design）未能执行。首要原因是其输入（需求分析产物）不存在或未被正确传递，导致该步骤没有可处理的内容。其次，该步骤本身也未生成任何输出文件。因此，该步骤完全未达到准出标准，必须被拒绝并重新执行。"
  },
  "raw": {
    "passed": false,
    "score": 0,
    "action": "reject",
    "critiques": [
      {
        "severity": "critical",
        "issue": "设计步骤的输入缺失。根据流程定义，设计步骤的输入是 `flows/requirement/output/`，但该目录为空，且未找到 `../project/docs/` 中的需求分析产物（如 `prd.md`, `user-stories.md`）。设计步骤缺少必要的输入，无法开始工作。",
        "suggestion": "必须先确保需求分析步骤（flows/requirement）已正确完成并将其产物（`../project/docs/prd.md`, `../project/docs/user-stories.md`）写入指定目录。请检查并重新运行需求分析步骤，或确认其输出路径是否正确。"
      },
      {
        "severity": "critical",
        "issue": "设计步骤未生成任何交付产物。根据准出标准，本步骤应在 `../project/docs/` 目录下生成 `architecture.md`, `api-design.md`, `ui-design.md` 三个核心设计文档，并在 `./output/` 目录下生成 `summary.md` 执行摘要。当前这些文件均不存在。",
        "suggestion": "在获得有效的输入后，必须执行设计工作，生成所有要求的交付文档。设计内容需覆盖所有P0功能模块，并满足其他准出标准。"
      }
    ],
    "verdict": "设计步骤（flows/design）未能执行。首要原因是其输入（需求分析产物）不存在或未被正确传递，导致该步骤没有可处理的内容。其次，该步骤本身也未生成任何输出文件。因此，该步骤完全未达到准出标准，必须被拒绝并重新执行。",
    "requiredFixes": [
      "确保输入可用：确认需求分析步骤（flows/requirement）已成功执行，并将其产物 `prd.md` 和 `user-stories.md` 正确写入 `../project/docs/` 目录。",
      "执行设计工作：在输入就绪后，基于需求分析产物，执行架构设计、API设计和UI设计，并将 `architecture.md`, `api-design.md`, `ui-design.md` 写入 `../project/docs/`，将 `summary.md` 写入 `./flows/design/output/`。"
    ]
  }
}