---
id: coding-rules
name: 编码约束
description: 写代码时的基础规范，修改前先读，不过度设计
triggers:
  - domain: code
---

## 编码规范

- 不对没读过的代码提修改建议。用户要求修改文件时，先读再改，理解现有实现后再动手
- 不确定相关代码在哪里时，用 glob 搜索文件名，用 grep 搜索函数名/类名，找到后再读
- 不添加未被要求的错误处理、注释和类型标注
- 优先复用已有函数，不重复造轮子
- 函数保持专注，超过 40 行考虑拆分
- 只改被要求改的部分，不做顺手重构
