# 2026-05-27 18:xx — Phase 10 R3-R8: Embedding adapters, document loader, knowledge_search, incremental indexer

## 变更

| Commit | 模块 | 内容 |
|--------|------|------|
| 639c468 | core/rag | R3 Embedding adapter implementations (OpenAI/Voyage/Local + factory) |
| 6aa32f3 | core/rag | R6 DocumentLoader — batch file indexing with chunking |
| a906ddf | core/tools | R7 knowledge_search tool — RAG vector search via ToolRegistry |
| f23723f | core/rag | R8 IncrementalIndexer — mtime-based file change detection |

## 详情

### R3: Embedding Adapter 实现

`packages/core/src/rag/embedding-adapter.ts`（334 行）：

- **OpenAIEmbeddingAdapter**：支持 text-embedding-3-small/large，自定义 baseUrl，批量处理
- **VoyageEmbeddingAdapter**：支持 voyage-3/voyage-3-lite/voyage-code-3
- **LocalEmbeddingAdapter**：hash-based 确定性嵌入（测试用），输出归一化单位向量
- **createEmbeddingAdapter 工厂**：按 provider 名称创建适配器
- 28 tests 全部通过

### R6: 文档加载器

`packages/core/src/rag/document-loader.ts`（298 行）：

- 支持 30+ 文件扩展名（Markdown/JSON/TypeScript/Python/Go/Rust 等）
- 自动分块：可配置 chunkSize/chunkOverlap，智能段落/句子边界检测
- 排除模式：node_modules/.git/dist 等默认排除
- 22 tests 全部通过

### R7: knowledge_search 工具

`packages/core/src/tools/knowledge-search.ts`（109 行）：

- 接收自然语言查询，通过 EmbeddingAdapter 转为向量
- 在 VectorStore 中执行相似度搜索
- 支持 topK/minScore/filter 参数
- 结果格式化为 markdown（含来源、分数、内容）
- 9 tests 全部通过

### R8: 增量索引器

`packages/core/src/rag/incremental-indexer.ts`（264 行）：

- **fullIndex()**：全量索引，清空后重建
- **incrementalIndex()**：增量更新，基于 mtime 检测变更
- 新增/修改/删除文件自动处理
- Manifest 导出/导入支持持久化
- 批量 embedding 计算（batch size 50）
- 10 tests 全部通过

## 测试总数

Core: 1221 → ~1290 tests（+69），Harness: 268 tests

## Roadmap 同步

- Phase 10 R3-R8 标记 ✅

## 遗留事项

- R9（RAG 集成测试）待实现
- session.test.ts 分页 flaky 测试为已知问题，非本次引入
- knowledge-search.test.ts 缺少 afterEach import 已修复
