# Changelog — 2026-05-27 23:xx

## Phase 17 AD2: Historical Success Rate — Windowed Stats, Trend Detection, Auto-Tuning

### Commit

| Hash | Module | Content |
|------|--------|---------|
| ac429dc | strategy | AD2: windowed stats, trend detection, auto-tuning, domain summary |

### Changes

**New types** (`types.ts`):
- `TrendDirection` — "improving" / "declining" / "stable" / "insufficient_data"
- `StrategyTrend` — trend analysis result with recent/older rates and delta
- `TimeWindow` — predefined windows: "1h" / "6h" / "24h" / "7d" / "30d"
- `DomainSummary` — domain-level overview (total strategies, overall success rate, best/worst)

**New methods** (`strategy-store.ts`):
- `getStatsWindowed(strategyId, window)` — time-windowed statistics
- `getStatsSince(strategyId, durationMs)` — arbitrary duration windowed stats
- `getTrend(strategyId, recentWindow, olderWindow, minRuns)` — trend detection comparing two windows
- `autoTune(promoteThreshold, deprecateThreshold, minRuns)` — auto-status transitions
- `getDomainSummary(domain)` — domain-level overview
- Refactored `getStats()` to share `computeStats()` helper with windowed variants

**Tests**: 17 new tests (windowed stats: 4, trend detection: 4, auto-status: 6, domain summary: 3)

### Roadmap Sync

- Phase 17 AD2 checkbox marked ✅

### Test Count

- Core: 2000 tests (107 files)
- Harness: 793 tests (40 files)
- Total: 2793 tests
