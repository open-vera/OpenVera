# 2026-05-27 · 19:xx — Phase 10.2 EV4：ToolBench 评测集集成

## 变更

| Commit | 模块 | 内容 |
|--------|------|------|
| (pending) | harness/eval | ToolBenchRunner — 16464 任务的工具使用评测集集成 |

### 新增文件

- `packages/harness/src/eval/runners/toolbench-runner.ts` — ToolBench Runner 实现
  - ToolBenchRawCase 格式解析（query/category/expected_tool_calls/difficulty）
  - 支持 single-tool / multi-tool / multi-turn 三类任务
  - 支持 optional tool 标记（不计入必须匹配的工具）
  - available_tools 注入到 prompt
  - ToolBenchMetrics：tool accuracy、per-category breakdown、avg API calls
- `packages/harness/src/eval/tests/toolbench-runner.test.ts` — 40 tests

### 修改文件

- `packages/harness/src/eval/runners/index.ts` — 导出 ToolBenchRunner 及类型

### 测试覆盖

- constructor 默认/自定义选项
- loadCases：category/difficulty/maxCases 组合过滤
- loadCasesFromJson：JSON 解析、非法 JSON 异常
- case 转换：category→level 映射、available_tools/context 注入、optional 工具排除
- runAll：正常执行、agent 异常处理、空 case 列表
- runAllWithMetrics：tool accuracy、per-category、avg API calls、pass rate
- evaluation：tool_match 评分、contains 评分、partial match
- edge cases：空 expected_tool_calls、全部 optional、缺失 difficulty、超长 query、大量工具

## Roadmap 同步

- EV4 checkbox 已标记 ✅

## 遗留事项

- session.test.ts 分页测试 flaky（已知问题，非本次引入）
