# 2026-04-29 · 11:xx — Smoke 入口统一

## 变更

| Commit | 模块 | 内容 |
|---|---|---|
| (pending) | core/agent/loop.ts | 修复 empty-after-tool 重试 bug：移除 `lastTurnHadToolResults = false`，使 MAX_EMPTY_AFTER_TOOL_RETRIES=3 生效 |
| (pending) | core/tests/repl-loop-smoke.test.ts | 新增 REPL loop smoke test（4 用例：正常流程、空回复恢复、重试耗尽、无工具结果不重试） |
| (pending) | core/package.json | 新增 `test:smoke:suite` 脚本，统一 smoke 入口 |
| (pending) | docs/core/p0-alignment-checklist.md | 标记 smoke 套件入口为已完成 |
| (pending) | docs/core/capability-gaps.md | 标记 REPL smoke 入口为已完成 |
| (pending) | docs/roadmap.md | 可靠性条目 🟡 → ✅ |

## Bug 详情

`loop.ts` 的 empty-after-tool 恢复路径中，`lastTurnHadToolResults = false` 导致重试条件在第一次重试后永远为 false，`MAX_EMPTY_AFTER_TOOL_RETRIES = 3` 形同虚设。修复后，loop 会在空回复后正确重试最多 3 次。

## Roadmap 同步

- `docs/roadmap.md:345` — 可靠性：🟡 → ✅
- `docs/core/p0-alignment-checklist.md:69` — [~] → [x]
- `docs/core/capability-gaps.md:41` — 标记 ✅

## 遗留事项

- `bash-tool.test.ts` 的 `kills stderr-heavy commands via streaming threshold` 超时（环境问题，非本次引入）
- `intent-tool-runtime.test.ts` 的 `shouldPlan` 断言失败（预存问题，非本次引入）
- coverage 因上述 2 个预存测试失败无法生成报告
