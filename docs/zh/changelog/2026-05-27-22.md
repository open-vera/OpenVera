# Changelog — 2026-05-27 22:xx

## Phase 16 CH5：Webhook Channel

### 变更

| Commit | 模块 | 内容 |
|--------|------|------|
| e3b6089 | channel | WebhookChannelAdapter — HTTP webhook 接收器 + HMAC-SHA256 签名验证 |

### 实现要点

- `packages/core/src/channel/webhook-channel.ts` — WebhookChannelAdapter 类
  - HTTP server 监听 webhook POST 请求
  - 支持 4 种签名验证策略：GitHub (X-Hub-Signature-256)、Stripe (Stripe-Signature)、Slack (X-Slack-Signature)、Custom
  - HMAC-SHA256 + timingSafeEqual 防止时序攻击
  - 内置 payload parser：GitHub push/issue 事件、Stripe 支付事件
  - 支持自定义 payload parser（WebhookPayloadParser 回调）
  - 可配置 path、methods、success status code、response body
- `packages/core/src/channel/tests/webhook-channel.test.ts` — 49 tests
  - Lifecycle: 6 tests（状态转换、幂等 connect、disconnect 安全）
  - Webhook receiving: 11 tests（default parser 4 种字段名、senderId 提取、422/400/404 响应、CORS、计数）
  - Custom path/methods: 3 tests
  - GitHub signature: 3 tests（有效签名、无效签名、缺少 header）
  - Stripe signature: 2 tests（有效签名、无效签名）
  - Slack signature: 2 tests（有效签名、无效签名）
  - Custom signature: 3 tests（有效签名、自定义 prefix、无效签名）
  - GitHub parser: 3 tests（push/issue/unknown events）
  - Stripe parser: 1 test
  - Custom parser: 3 tests（自定义 parser、null 返回、headers 传递）
  - sendMessage: 2 tests（未连接抛错、outbound 记录）
  - onMessage: 1 test（unsubscribe）
  - getHistory: 2 tests（senderId 过滤、limit）
  - Custom response: 2 tests（自定义 status code、自定义 body）
  - Edge cases: 3 tests（默认 senderId、自定义 senderId、多 callback）

### Roadmap 同步

- [x] CH5 Webhook Channel — 完成

### 测试总数

- Core: 1900 tests（含 49 webhook channel tests）
- Harness: 732 tests
- 总计: 2632 tests

### 遗留事项

- CH7 Channel 测试 — Gateway 生命周期、消息路由、多 channel 并发集成测试

---

## Phase 16 CH6：Channel Plugin Registry

### 变更

| Commit | 模块 | 内容 |
|--------|------|------|
| 311a792 | channel | ChannelPluginRegistry — 运行时动态加载/卸载 channel adapter |

### 实现要点

- `packages/core/src/channel/plugin-registry.ts` — ChannelPluginRegistry 类
  - `registerPlugin()` / `unregisterPlugin()` — 注册/注销 adapter 工厂（带元数据）
  - `loadAdapter()` / `unloadAdapter()` — 运行时动态创建/销毁 adapter 实例
  - `loadBatch()` — 批量加载多个实例
  - `unloadAllByPlugin()` — 按 plugin 卸载所有实例
  - `unloadAll()` — 卸载全部（自动 disconnect 已连接的）
  - `findByChannelType()` — 按 channel 类型查找插件
  - 4 种错误类：PluginAlreadyRegistered / PluginNotFound / AdapterAlreadyLoaded / AdapterNotLoaded
  - Plugin 元数据：name, description, channelType, version, author
  - LoadedAdapter 跟踪：pluginName, adapter, loadedAt, config
- `packages/core/src/channel/tests/plugin-registry.test.ts` — 42 tests
  - Plugin registration: 3 tests（注册、重复报错、多插件）
  - Plugin unregistration: 3 tests（注销、不存在、自动卸载关联 adapter）
  - Plugin querying: 5 tests（hasPlugin、getPlugin、listPlugins、findByChannelType）
  - Adapter loading: 5 tests（成功、插件不存在、实例名重复、多插件加载、多实例）
  - Adapter unloading: 4 tests（成功、不存在、已连接自动断开、未连接不断开）
  - getLoadedAdapter / listLoadedAdapters: 4 tests
  - Batch operations: 4 tests（loadBatch、unloadAllByPlugin、unloadAll）
  - Introspection: 2 tests（pluginCount、adapterCount）
  - Error classes: 4 tests

### Roadmap 同步

- [x] CH6 Channel 插件注册 — 完成

### 测试总数

- Core: 1942 tests（含 42 plugin registry tests）
- Harness: 732 tests
- 总计: 2674 tests

### 遗留事项

- CH7 Channel 测试 — Gateway 生命周期、消息路由、多 channel 并发集成测试

---

## Phase 16 CH7：Channel Multi-Channel Concurrent Tests

### 变更

| Commit | 模块 | 内容 |
|--------|------|------|
| abe980a | channel | Multi-channel concurrent scenario tests — 8 integration tests |

### 实现要点

- `packages/core/src/channel/tests/gateway.test.ts` — +8 tests（multi-channel lifecycle describe block）
  - Full lifecycle management across 3 channels（connect → message → disconnect）
  - Correct channel name routing from different adapters
  - Concurrent message dispatch from multiple channels（Promise.all 4-way）
  - Event ordering verification during multi-channel operations
  - Error isolation between channels（one fails, others unaffected）
  - Cross-channel session bindings with correct associations
  - Partial connect failures in connectAll（graceful degradation）
  - Adapter removal while other channels are active

### Roadmap 同步

- [x] CH7 Channel 测试 — 完成（Phase 16 Channel 全部完成）

### 测试总数

- Core: 2000 tests（含 49 gateway tests = 41 existing + 8 new multi-channel）
- Harness: 732 tests
- 总计: 2732 tests

---

## Phase 17 AD1：Strategy Store — 策略仓库

### 变更

| Commit | 模块 | 内容 |
|--------|------|------|
| (pending) | strategy | StrategyStore — 按任务域存储 prompt/model/tool policy 配置 |

### 实现要点

- `packages/harness/src/strategy/types.ts` — 策略类型定义
  - 10 种 StrategyDomain（coding/debugging/research/writing/data-analysis/planning/review/testing/devops/general）
  - 4 种 StrategyStatus（active/deprecated/candidate/retired）
  - ModelConfig / ToolPolicy / PromptTemplate 配置类型
  - Strategy / StrategyOutcome / StrategyStats / Comparison 数据结构
- `packages/harness/src/strategy/strategy-store.ts` — StrategyStore 类
  - CRUD：add / get / update（自动 version++）/ remove（级联清除 outcomes）
  - 过滤：list（domain/status/tags/since）、getActiveByDomain、getBestForDomain（minRuns 阈值）
  - 成果追踪：recordOutcome / getOutcomes / getStats / getAllStats
  - 比较：compare（两策略对比 + confidence 计算）
  - 查询：count / countByStatus / countByDomain / findUnderperforming
  - 持久化：JSON 文件（strategies.json + strategy-outcomes.json），容错加载
- `packages/harness/src/strategy/index.ts` — barrel export
- 更新 `packages/harness/src/index.ts` — 导出 strategy 模块
- `packages/harness/src/strategy/tests/strategy-store.test.ts` — 44 tests
  - CRUD: 7 tests（add/get/update/remove + 重复 ID / 幂等 id / version 递增）
  - 过滤: 7 tests（domain/status/tags/since/组合/activeByDomain）
  - 成果追踪: 3 tests
  - 统计: 6 tests（successRate/avgDuration/totalTokens/zero stats/lastRun/getAllStats）
  - 最优策略: 4 tests（successRate/minRuns/无活跃/回退）
  - 比较: 3 tests（winner/confidence/missing）
  - 查询: 4 tests（count/byStatus/byDomain/underperforming）
  - 持久化: 4 tests（disk/corrupted strategies/corrupted outcomes/missing dir）
  - 边界: 4 tests（empty tags/no tags/no token usage/all domains）

### Roadmap 同步

- [x] AD1 策略仓库 — 完成

### 测试总数

- Core: 2000 tests
- Harness: 776 tests（+44 strategy tests）
- 总计: 2776 tests
